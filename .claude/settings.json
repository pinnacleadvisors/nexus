{
  "permissions": {
    "allow": [
      "Bash(doppler *)",
      "Bash(npm install -g doppler)",
      "Bash(brew install doppler)",
      "Read",
      "Write",
      "Edit",
      "Bash"
    ]
  }
}
