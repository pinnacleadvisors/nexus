{
  "permissions": {
    "allow": [
      "Bash(doppler *)",
      "Bash(npm install -g doppler)",
      "Bash(brew install doppler)",
      "Read",
      "Write",
      "Edit"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Glob|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f graphify-out/GRAPH_REPORT.md ] && echo 'graphify: knowledge graph exists — read graphify-out/GRAPH_REPORT.md for god nodes and module communities before searching raw files.' || true"
          }
        ]
      }
    ]
  }
}
