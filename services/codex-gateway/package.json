{
  "name": "@nexus/codex-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted Codex CLI gateway for Nexus. Drains the user's ChatGPT Pro plan via the codex CLI and exposes the same HMAC protocol as services/claude-gateway, so dispatchToCodexGateway can use it as a sandboxed manual-ops AI runtime.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@hono/node-server": "1.19.13",
    "hono": "4.12.15",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "tsx": "4.21.0",
    "typescript": "5.7.2"
  }
}
