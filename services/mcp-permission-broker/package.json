{
  "name": "@nexus/mcp-permission-broker",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server that catches CLI tool-permission requests inside the claude-gateway and surfaces them to the operator's chat UI as Allow/Deny cards. Implements the `permission_prompt` tool consumed by Claude Code's `--permission-prompt-tool` flag.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "tsx": "^4.21.0",
    "typescript": "5.7.2"
  }
}
