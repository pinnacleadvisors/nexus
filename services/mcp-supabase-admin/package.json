{
  "name": "@nexus/mcp-supabase-admin",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing Supabase admin tools to platform-copilot. SELECT-only queries fire freely; mutating SQL (UPDATE/INSERT/DELETE/DDL) returns requires_approval so the agent escalates via approval-request blocks. Audits every call via lib/audit/tool-call.ts.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "tsx": "^4.21.0",
    "typescript": "5.7.2"
  }
}
